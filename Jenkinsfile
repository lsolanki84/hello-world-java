pipeline {
    	agent { label 'jenkins-mvn'}
	stages {
        stage('Build') {
                    steps {
			    script {
				
				sh "sleep 3600"
                        }
		    }
        }
    }
}

